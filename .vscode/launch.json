{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "request": "launch",
      "name": "Run",
      "cwd": "${workspaceFolder}",
      "program": "${workspaceFolder}/myshell",
      // "args": ["-d", "input_enc.txt"],
      "preLaunchTask": "C/C++: gcc build active file"
    }
  ]
}
